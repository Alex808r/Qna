$('.question-errors').html('<%= render 'shared/errors', resource: @question %>');

<% unless @question.errors.present? %>
//  $('.question').html('<%= j render @question %>');
  $('#question-list-item-<%= @question.id %>').remove()
  $('.question').append('<%= j render @question %>');
<% end %>